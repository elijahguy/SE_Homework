// Define a funtion maxOFTwoNumbers that takes two numbers as arguments and returns the largest of them.
function max(firstNum, secondNum){
  
    if (firstNum > secondNum) {
       console.log(firstNum + " is larger than " + secondNum);
    } else {
        console.log(firstNum + " is less than " + secondNum);
    }
    return;
  
}

// two numbers 
max (86,150)




